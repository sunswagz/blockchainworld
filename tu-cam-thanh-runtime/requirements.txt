# Cố ý ít phụ thuộc. Chỉ báo viết tay bằng Python thuần (400 nến thì numpy/pandas
# không mua lại được gì, mà lại thêm hai thứ có thể cài hỏng).
anthropic>=0.122
fastapi>=0.141
uvicorn[standard]>=0.52
