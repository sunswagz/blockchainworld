# KIẾN TRÚC
`PDF → source locator → concept → relation → economic role → repo bridge → hall context`

Ba lớp không được ghi đè nhau: **BOOK** (tác giả nói), **REALITY 2026** (đo được), **SUNSWaGz MODEL** (kiến trúc/chọn lựa). Knowledge OS không ra lệnh giao dịch; quyết định vốn vẫn đi `Opportunity → Risk Engine → Capital Allocator → Execution`.
